# ALDITalk-Helper
Ein Bot der es automatisiert Datenvolumen bei ALDITalk nachzubuchen anstatt es manuell per App zu machen.

# Requirements:
`requests`\
`json5`\
`curl_cffi`\
`playwright`\
`ruff`

# Tutorial:

 1. `pip install -r requirements.txt`
 2. `config.json5` konfigurieren
